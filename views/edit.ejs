<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write It Out...</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="px-3 py-2 bg-red-500 rounded-md text-sm mb-5 inline-block">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post"> 
            <textarea class=" w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block" placeholder="What's on your mind?" name="content"><%= post.content%></textarea>
            <input class="px-3 py-2 w-40 text-sm bg-yellow-500 text-black rounded-md block mt-2" type="submit" value="Update post!!">
        </form>
    </div>
</body>
</html>