<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write It Out...</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="px-3 py-2 bg-red-500 rounded-md text-sm mb-5 inline-block">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-12 h-12  rounded md">
                <img class="w-full h-full object-cover overflow-hidden" src="/images/uploads/<%= user.profilepic%>" alt="">
            </div>
            <h3 class="text-3xl mb-5">Hello, <%= user.name%>👋</h3>
        </div>
        <a
            href="/profile/upload"
            class="inline-block mt-4 px-3 py-2 bg-yellow-500 text-white rounded-md"
        >Update Profile
        </a>
        <h5 class="mb-5 mt-3">You can create a new post here....</h5>
        <form action="/post" method="post"> 
            <textarea class=" w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block" placeholder="What's on your mind?" name="content"></textarea>
            <input class="px-3 py-2 w-40 text-sm bg-blue-500 rounded-md block mt-2" type="submit" value="Create new post!!">
        </form>
        <div class="posts mt-20">
            <h3>Your Posts...</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 border-[1px] border-zinc-800 rounded-md bg-zinc-700 mb-4">
                    <h4 class="text-blue-500 mb-2">@<%= user.username%>></h4>
                    <p class="text-sm tracking-tight"><%=post.content%></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns mt-1 flex gap-4">
                        <a class="text-blue-500 " href="/like/<%= post._id%>">
                            <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike'%>
                        </a>
                        <a class="text-yellow-500" href="/edit/<%= post._id%>">Edit</a>
                        <form action="/delete/<%= post._id %>" method="POST">
                            <button type="submit" class="text-red-500">Delete</button>
                        </form>
                    </div>
                </div>
                <% }) %>
            </div>

        </div>
    </div>
</body>
</html>